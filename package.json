{
  "private": true,
  "scripts": {
    "build": "cd packages/steiner && npm run build",
    "postinstall": "lerna bootstrap",
    "seed:demo": "rimraf packages/steiner-demo/src/modules/offers && cd packages/steiner-cli && node index offers -o ../steiner-demo/src/modules",
    "start:demo": "cd packages/steiner-demo && npm start"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.29",
    "react": "^15.3.2",
    "react-redux": "^4.4.5",
    "redux": "^3.6.0",
    "redux-form": "^6.0.5",
    "rimraf": "^2.5.4"
  }
}
