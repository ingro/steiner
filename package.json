{
  "private": true,
  "scripts": {
    "build": "cd packages/steiner && npm run build",
    "postinstall": "lerna bootstrap",
    "seed:demo": "rimraf packages/steiner-demo/src/modules/offers && cd packages/steiner-cli && node index generate offers -o ../steiner-demo/src/modules",
    "start:demo": "cd packages/steiner-demo && npm start",
    "publish": "lerna publish && lerna-npm-readme-hack --packages steiner,steiner-cli",
    "bootstrap": "node scripts/clean && lerna bootstrap && node scripts/link"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.30",
    "lerna-npm-readme-hack": "^1.0.6",
    "react": "^15.3.2",
    "react-redux": "^5.0.0-beta.3",
    "react-router": "^4.0.0-alpha.5",
    "react-router-addons-controlled": "0.0.1",
    "redux": "^3.6.0",
    "redux-form": "^6.1.1",
    "rimraf": "^2.5.4"
  }
}
