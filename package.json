{
  "private": true,
  "scripts": {
    "build": "cd packages/steiner && npm run build",
    "postinstall": "lerna bootstrap",
    "seed:demo": "rm -r packages/steiner-demo/src/modules/hotels && cd packages/steiner-cli && node index hotels -o ../steiner-demo/src/modules",
    "start:demo": "cd packages/steiner-demo && npm start"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.29",
    "react": "^15.3.2",
    "react-redux": "^4.4.5",
    "redux": "^3.6.0",
    "redux-form": "^6.0.5"
  }
}
